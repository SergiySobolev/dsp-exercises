L = 256;
A = 256;

a = -(log(1/2)/(L*L));

I = imread('coins.png');
figure
subplot(3,1,1)
imshow(I,'InitialMagnification', 400);
 
r = 1:1:700;
t = A*exp(-a*(r.*r));
TI = t(I);

subplot(3,1,2)
imshow(uint8(TI));

subplot(3,1,3)
plot(r,t);
hold on;
plot(r(L),y(10),'r*')
